yolo_model:

  config_file:
    name: yolov3-tiny-traffic-new.cfg
  weight_file:
    name: yolov3-tiny-traffic-new_last.weights
    # name: yolov3-tiny-dp_last.weights
    # name: yolov3-tiny-dp_best.weights
  threshold:
    value: 0.3
  detection_classes:
    names:
      - school
      - turn
      - slow
      - straight
      - stop
      - bus
      - parking
      - crossing_sign
      - construction
      - crossing
